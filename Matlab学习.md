---
cssclasses:
  - 笔记
---
- format long
- format short
- 矩阵使用`[]`创建
- 串联使用`[]`
- 矩阵乘法`*`  
- 元素级乘法`.*` 
- 元素级幂`.^`
- 矩阵转置 (复数矩阵转置加共轭)`'` 
- 复数矩阵转置不共轭`.'`
- 矩阵索引与其他语言不同，从1开始，使用`()`索引
- 矩阵索引使用单一下标则按顺序向下遍历每一列
- 矩阵切片使用`:`
- 冒号运算符还允许您使用较通用的格式 `start:step:end` 创建等间距向量值，有`start`值不一定有`end`值
- 函数`linspace(start,end,num)`，有`start`值不一定有`end`值
- 保存工作区变量`save name.mat`
- 加载MAT文件`load name.mat`
- 使用`whos value`可以查看变量属性
- 字符串`""`,如果文本包含双引号，则定义中使用两个双引号
- 要将文本添加到字符串的末尾，请使用加号运算符 `+`(合并行)，如果串联不会合并行
- 字符串是整体，无法索引内部元素
- 字符数组`''`，串联会合并行，保留列，可以用`()`索引内部元素
- `strlength(A)`可以求得长度
- 输出文本使用`disp()`
- `hold on`
- `hold off`
- `tiledlayout`该函数比 `subplot` 提供更多对标签和间距的控制。如在图窗窗口中创建 2×2 布局。每当您要某个绘图出现在下一区域中时，请调用 `nexttile`


